# 오늘의 냉장고 운영 안전 절차

## 백업

1. Supabase Dashboard의 Database Backups에서 자동 백업 보관 상태를 확인한다.
2. 대규모 상품 수정이나 SQL 마이그레이션 전에는 수동 백업을 만든다.
3. Storage의 `product-images`, `review-images` 버킷은 DB 백업과 별개이므로 정기적으로 별도 복사한다.
4. `.env`와 서비스 롤 키는 백업 파일에 넣지 않고 비밀 관리자에 별도 보관한다.

## 복구

1. 장애 시간과 마지막 정상 변경 시간을 `admin_audit_logs`, `order_change_logs`에서 확인한다.
2. 운영 프로젝트를 바로 덮어쓰기 전에 별도 Supabase 프로젝트에 백업을 복원한다.
3. 주문 수량, 결제 상태, `bundle_items.stock_quantity`, 알림 수를 표본 대조한다.
4. 검증 후 Supabase의 Point-in-Time Recovery 또는 백업 복원 절차를 실행한다.
5. 복구 완료 후 서버를 재시작하고 `/admin-system-status.html`에서 전체 항목을 점검한다.

## 동시 주문 충돌 점검

- 같은 상품의 마지막 재고를 두 계정이 동시에 주문하는 시나리오를 테스트한다.
- 같은 브라우저에서 신청 버튼을 빠르게 두 번 눌러도 `request_key` 때문에 주문은 한 건만 생성돼야 한다.
- 재고는 음수가 되면 안 되며, 주문 수량과 재고 차감 수량이 일치해야 한다.
- 취소와 주문이 동시에 실행될 때 대기자 선착순과 부분 배정 수량을 확인한다.

## 입금 대기 자동 만료

- 기본값은 보따리 신청 마감 시각에 입금 미확인 계좌이체 주문을 자동 취소하는 것이다.
- `.env`의 `PAYMENT_EXPIRY_GRACE_MINUTES`로 유예 시간을 줄 수 있다.
- 자동 취소도 주문 변경 이력과 관리자 감사 이력에 남으며 재고는 대기자에게 먼저 배정된다.

## 이미지 제한

- 상품 이미지는 최대 30장, 후기 이미지는 최대 10장이다.
- 한 장당 5MB 이하의 JPEG, PNG, WebP, GIF만 허용한다.
- 실제 파일은 Supabase Storage에 저장하고 DB에는 공개 URL만 저장한다.
